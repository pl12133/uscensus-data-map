const expect = require('chai').expect;

describe('Tests', () => {
  it('should be written in advance', () => {
    expect(true).to.be.ok;
  });
  it('should be written daily', () => {
    expect(true).to.be.ok;
  });
  it('should be written now', () => {
    expect(true).to.be.ok;
  });
  describe('Chai', () => {
    it('is your friend!', () => {
      expect(expect).to.be.ok;
    });
  });
});
